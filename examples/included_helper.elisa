def included_work(limit: i64) -> i64:
    result: mutable i64 = 0
    index: mutable i64 = 0
    while index < limit |result, index|:
        result <- result + index
        index <- index + 1
    return result
